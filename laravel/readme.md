##Install packages 

```
npm i --save-dev ably axios bootbox bootstrap cross-env font-awesome jquery laravel-mix lodash popper.js sass sass-loader vue vue-resource vue-template-compiler sweetalert
```